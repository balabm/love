feat(modern): Notification Filter — contextual relevance filtering

Notification Filter (core/notification_filter.py):
- filter_notifications(): Scores notifications by relevance using
  priority boost, source importance (learned), category alignment,
  time sensitivity, and focus mode suppression
- Determines action: deliver (high relevance), batch (medium),
  defer (wrong time), suppress (low relevance or focus mode)
- Learns from user interactions: boosts sources user acts on,
  penalizes ignored sources
- Adapts to work hours (defers personal notifications) and
  focus mode (suppresses non-urgent)
- 264 lines of production notification filtering infrastructure

API Endpoints (api/modern_routes.py):
- POST /modern/notifications/filter — Filter notifications by context
- POST /modern/notifications/feedback — Record interaction feedback
- GET /modern/notifications/filter/stats — Filter statistics

Integration:
- AGI spine includes notification_filter flag
- /evolution/health and /intelligence/self-evolution include availability
- start_evolution.py lists Notification Filter

All 101 tests pass.

Generated with [Devin](https://cli.devin.ai/docs)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
