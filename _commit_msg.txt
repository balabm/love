feat(security): Rate Limiter — API abuse protection with token bucket

Rate Limiter (api/rate_limiter.py):
- Token bucket algorithm with per-client buckets and burst tolerance
- Endpoint-specific categories: heavy (graph/vector), medium (predict/analyze),
  light (health/stats), auth (login)
- check_rate_limit(): Consume tokens, return remaining + reset time
- get_category_for_endpoint(): Auto-categorize endpoints by path
- 172 lines of production rate limiting infrastructure

API Endpoints (api/modern_routes.py):
- GET /modern/rate-limit/status — Client rate limit status
- GET /modern/rate-limit/stats — Global rate limiter statistics

All 95 tests pass.

Generated with [Devin](https://cli.devin.ai/docs)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
